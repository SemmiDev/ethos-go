services:
  # ============================================================================
  # Application Services
  # ============================================================================
  app:
    container_name: ethosgo-app
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
        COMMIT: ${COMMIT:-unknown}
        BUILD_TIME: ${BUILD_TIME:-unknown}
    ports:
      - '8080:8080'
    env_file:
      - .env
    networks:
      - ethosgo-network
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    depends_on:
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
      tempo:
        condition: service_healthy
      loki:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:8080/health']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  worker:
    container_name: ethosgo-worker
    build:
      context: .
      dockerfile: Dockerfile.worker
    env_file:
      - .env
    networks:
      - ethosgo-network
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    depends_on:
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ============================================================================
  # Database Services
  # ============================================================================
  postgresql:
    container_name: ethosgo-postgresql
    image: postgres:17-alpine
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - ethosgo-postgresql-data-volume:/var/lib/postgresql/data
    networks:
      - ethosgo-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-ethosgo}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  redis:
    container_name: ethosgo-redis
    image: redis:8.0-alpine
    ports:
      - '6379:6379'
    volumes:
      - ethosgo-redis-data-volume:/data
    networks:
      - ethosgo-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'redis-cli', '--raw', 'incr', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    depends_on:
      - postgresql

  # ============================================================================
  # Messaging Services
  # ============================================================================
  nats:
    container_name: ethosgo-nats
    image: nats:2.10-alpine
    ports:
      - '4222:4222'   # NATS Client
      - '8222:8222'   # HTTP Monitoring
    command: ["--jetstream", "--store_dir", "/data", "-m", "8222"]
    volumes:
      - ethosgo-nats-data-volume:/data
    networks:
      - ethosgo-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ============================================================================
  # LGTM Stack: Loki (Logs), Grafana (Visualization), Tempo (Traces), Prometheus (Metrics)
  # ============================================================================

  # Loki - Log aggregation
  loki:
    container_name: ethosgo-loki
    image: grafana/loki:3.0.0
    ports:
      - '3100:3100'
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./observability/loki-config.yaml:/etc/loki/local-config.yaml:ro
      - loki-data:/loki
    networks:
      - ethosgo-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:3100/ready']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Promtail - Log shipper (collects logs and sends to Loki)
  promtail:
    container_name: ethosgo-promtail
    image: grafana/promtail:3.0.0
    command: -config.file=/etc/promtail/config.yaml
    volumes:
      - ./observability/promtail-config.yaml:/etc/promtail/config.yaml:ro
      - ./logs:/app/logs:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - ethosgo-network
    restart: unless-stopped
    depends_on:
      loki:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:9080/ready']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # Tempo - Distributed tracing backend
  tempo:
    container_name: ethosgo-tempo
    image: grafana/tempo:2.4.0
    ports:
      - '3200:3200' # Tempo HTTP
      - '4317:4317' # OTLP gRPC
      - '4318:4318' # OTLP HTTP
    command: ['-config.file=/etc/tempo/tempo.yaml']
    volumes:
      - ./observability/tempo-config.yaml:/etc/tempo/tempo.yaml:ro
      - tempo-data:/tmp/tempo
    networks:
      - ethosgo-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:3200/ready']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Prometheus - Metrics collection and storage
  prometheus:
    container_name: ethosgo-prometheus
    image: prom/prometheus:v2.50.0
    ports:
      - '9090:9090'
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--storage.tsdb.retention.size=10GB'
      - '--web.enable-remote-write-receiver'
      - '--web.enable-lifecycle'
      - '--enable-feature=exemplar-storage'
    volumes:
      - ./observability/prometheus.yaml:/etc/prometheus/prometheus.yml:ro
      - ./observability/rules:/etc/prometheus/rules:ro
      - prometheus-data:/prometheus
    networks:
      - ethosgo-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:9090/-/healthy']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # Grafana - Unified observability dashboard
  grafana:
    container_name: ethosgo-grafana
    image: grafana/grafana:10.4.0
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - GF_FEATURE_TOGGLES_ENABLE=traceqlEditor tempoSearch tempoBackendSearch tempoServiceGraph traceToMetrics correlations
    volumes:
      - ./observability/grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
      - ./observability/grafana-dashboards.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml:ro
      - ./observability/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    networks:
      - ethosgo-network
    restart: unless-stopped
    depends_on:
      loki:
        condition: service_healthy
      tempo:
        condition: service_healthy
      prometheus:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:3000/api/health']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

networks:
  ethosgo-network:
    driver: bridge

volumes:
  ethosgo-postgresql-data-volume:
    driver: local
  ethosgo-redis-data-volume:
    driver: local
  ethosgo-nats-data-volume:
    driver: local
  loki-data:
    driver: local
  tempo-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local
